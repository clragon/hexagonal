// Hexagonal docs site entry. The lib (dist/hexagonal.js) is loaded separately
// on the page and registers all <hex-*> custom elements. We just register the
// book chrome elements here.

import "./book-shell.js";
import "./component-page.js";
import "./playground.js";
